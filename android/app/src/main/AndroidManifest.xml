<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link de auth (OAuth Google/Apple + reset de senha).
                 Esquema custom com.caminare.app:// — tratado pelo @capacitor/app
                 e entregue ao Supabase pela NativeAuthBridge. Deve constar na
                 allow-list de Redirect URLs do Supabase. -->
            <intent-filter android:autoVerify="false">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.caminare.app" />
            </intent-filter>

            <!-- App Links (https) p/ o domínio de produção — camada extra ao esquema
                 custom acima. Abre o app direto nos links de OAuth/reset servidos por
                 https, verificado via /.well-known/assetlinks.json (autoVerify=true).
                 ⚠️ TROCAR __DOMINIO_PROD__ pelo host real (ex.: caminare.com.br) e
                 publicar o assetlinks.json com o SHA-256 da chave de release ANTES de
                 enviar pra loja. A NativeAuthBridge já trata estes paths. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="__DOMINIO_PROD__" android:pathPrefix="/auth-callback" />
                <data android:scheme="https" android:host="__DOMINIO_PROD__" android:pathPrefix="/reset-callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Gravação por voz com transcrição nativa (@capacitor-community/speech-recognition).
         RECORD_AUDIO é exigida pelo SpeechRecognizer. Nenhum áudio é gravado/guardado:
         o sistema transcreve e nós só recebemos o texto. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Android 11+ (package visibility): permite localizar o serviço de
         reconhecimento de fala do aparelho. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
